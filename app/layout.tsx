import type { Metadata } from 'next';
import { Inter } from 'next/font/google';
import './globals.css';
import { Providers } from './providers';

const inter = Inter({ subsets: ['latin'] });

export const metadata: Metadata = {
  title: 'RightsGuard - Know Your Rights, Stay Safe',
  description: 'A mobile-first app for individuals to quickly access legal rights information during police interactions and document incidents.',
  keywords: 'legal rights, police interactions, civil rights, safety, documentation',
  authors: [{ name: 'RightsGuard Team' }],
  viewport: 'width=device-width, initial-scale=1',
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        <Providers>
          {children}
        </Providers>
      </body>
    </html>
  );
}
